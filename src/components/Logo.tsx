import React from 'react';

export default function Logo() {
  return (
    <div className="inline-flex flex-col leading-tight">
      <span className="text-3xl font-semibold tracking-tight">DMX</span>
      <span className="text-xs uppercase tracking-[0.2em] text-zinc-400">O₂ | He</span>
    </div>
  );
}
